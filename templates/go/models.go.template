// Generated by Sterling SDK Generator
// {{spec_title}} v{{spec_version}}

package {{go_package_name}}

{{#each models}}
{{#if is_enum}}
{{#if description}}
// {{description}}
{{/if}}
type {{name}} string

const (
{{#each enum_values}}
	{{name}}{{pascal_value}} {{name}} = "{{value}}"
{{/each}}
)

{{else}}
{{#if has_properties}}
// {{description}}
type {{name}} struct {
{{#each properties}}
{{#if description}}
	// {{description}}
{{/if}}
	{{pascal_name}} {{go_type}} `json:"{{name}}{{#unless required}},omitempty{{/unless}}"`
{{/each}}
}

{{else}}
type {{name}} struct {
	Data interface{} `json:"data,omitempty"`
}

{{/if}}
{{/if}}
{{/each}}
