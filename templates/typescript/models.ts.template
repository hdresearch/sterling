// Generated by Sterling SDK Generator
// {{spec_title}} v{{spec_version}}

{{#each models}}
{{#if is_enum}}
export type {{name}} = {{#each enum_values}}"{{value}}"{{/each}};

{{else}}
export interface {{name}} {
{{#each properties}}
  {{name}}{{#unless required}}?{{/unless}}: {{ts_type}};
{{/each}}
}

{{/if}}
{{/each}}
