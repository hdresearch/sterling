# {{package_name}}

TypeScript SDK for **{{spec_title}}** v{{spec_version}}.

Generated by [Sterling SDK Generator](https://github.com/hdresearch/sterling).

## Installation

```bash
npm install {{package_name}}
```

## Quick Start

```typescript
import { {{class_name}}Client } from '{{package_name}}';

const client = new {{class_name}}Client({
  baseUrl: '{{base_url}}',
  apiKey: 'your-api-key',
});

// Example usage
{{#each operations}}
// {{summary}}
const {{camel_name}}Result = await client.{{camel_name}}({{#if has_body}}{ /* request body */ }{{/if}});
console.log({{camel_name}}Result.data);

{{/each}}
```

## Configuration

```typescript
const client = new {{class_name}}Client({
  baseUrl: '{{base_url}}',   // API base URL
  apiKey: 'your-api-key',       // API key for authentication
  timeout: 30000,                // Request timeout in ms
  headers: {                     // Custom headers
    'X-Custom-Header': 'value',
  },
});
```

## API Methods

{{#each operations}}
### `{{camel_name}}()`

{{summary}}

- **Method:** `{{method}}`
- **Path:** `{{path}}`

{{/each}}

## License

MIT
