# Generated by Sterling SDK Generator
# {{spec_title}} v{{spec_version}}

import httpx
from typing import Any, Optional


class {{class_name}}Client:
    """Client for {{spec_title}}"""

    def __init__(self, base_url: str = "{{base_url}}", api_key: Optional[str] = None):
        self.base_url = base_url
        self.api_key = api_key
        headers = {}
        if api_key:
            headers["Authorization"] = f"Bearer {api_key}"
        self._client = httpx.AsyncClient(base_url=base_url, headers=headers)

{{#each operations}}
    async def {{snake_name}}(self{{#if has_path_params}}, {{path_params_py}}{{/if}}{{#if has_body}}, body: {{#if has_typed_body}}dict[str, Any]{{else}}dict[str, Any] | None = None{{/if}}{{/if}}{{#if has_query_params}}{{query_params_py}}{{/if}}) -> httpx.Response:
        """{{summary}}"""
{{#if has_path_params}}
        url = f"{{path_interpolate_py}}"
{{else}}
        url = "{{path}}"
{{/if}}
{{#if has_query_params}}
        params: dict[str, Any] = {}
{{#each query_params}}
        if {{name}} is not None:
            params["{{name}}"] = {{name}}
{{/each}}
        return await self._client.request("{{method}}", url{{#if has_body}}, json=body{{/if}}, params=params)
{{else}}
        return await self._client.request("{{method}}", url{{#if has_body}}, json=body{{/if}})
{{/if}}

{{/each}}
    async def close(self):
        await self._client.aclose()

    async def __aenter__(self):
        return self

    async def __aexit__(self, *args):
        await self.close()
