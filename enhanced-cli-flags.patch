--- src/main.zig.orig
+++ src/main.zig
@@ -15,9 +15,12 @@
     std.debug.print("  sterling generate --spec <openapi.yaml> --config <sterling.toml> [--enhance]\n", .{});
     std.debug.print("  sterling webhook --port <port> --config <sterling.toml>\n", .{});
     std.debug.print("\nOptions:\n", .{});
-    std.debug.print("  --enhance                        # Enable LLM code enhancement\n", .{});
+    std.debug.print("  --enhance                        # Enable LLM code enhancement\n", .{});
+    std.debug.print("  --docs                           # Generate documentation\n", .{});
+    std.debug.print("  --github                         # Create GitHub repositories\n", .{});
+    std.debug.print("  --publish                        # Publish packages\n", .{});
     std.debug.print("\nEnvironment Variables:\n", .{});
     std.debug.print("  ANTHROPIC_API_KEY               # For LLM enhancement\n", .{});
     std.debug.print("  GITHUB_TOKEN                    # For GitHub automation\n", .{});
 }

@@ -30,6 +33,9 @@
     var config_path: ?[]const u8 = null;
     var spec_path: ?[]const u8 = null;
     var enhance_flag = false;
+    var docs_flag = false;
+    var github_flag = false;
+    var publish_flag = false;
     var port: u16 = 8080;
 
     var i: usize = 1;
@@ -47,6 +53,12 @@
             i += 1;
         } else if (std.mem.eql(u8, args[i], "--enhance")) {
             enhance_flag = true;
+        } else if (std.mem.eql(u8, args[i], "--docs")) {
+            docs_flag = true;
+        } else if (std.mem.eql(u8, args[i], "--github")) {
+            github_flag = true;
+        } else if (std.mem.eql(u8, args[i], "--publish")) {
+            publish_flag = true;
         } else if (std.mem.eql(u8, args[i], "--port")) {
             if (i + 1 >= args.len) {
                 std.debug.print("Error: --port requires a value\n", .{});
